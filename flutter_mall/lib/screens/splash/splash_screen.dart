import 'package:flutter/material.dart';
import 'package:flutter_mall/screens/splash/components/body.dart';

class SplashScreen extends StatelessWidget {
  static String roteName = "/splash";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body()
    );
  }
}